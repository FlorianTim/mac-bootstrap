
# Modules (Brewfiles)

Brewfiles are split by topic:

## Everyday modules

* Brewfile.base â€” CLI essentials and small utilities
* Brewfile.office â€” office/PDF/email/sync tools
* Brewfile.browser-media â€” browsers and multimedia/photo tools

## Developer modules

* Brewfile.dev-java â€” Java (Temurin 21) + jenv + Maven/Gradle + Kotlin tooling
* Brewfile.dev-android â€” Android Studio + SDK tools + adb/NDK + debugging helpers
* Brewfile.dev-flutter-cordova â€” Flutter (brew cask) + Node.js + helpers
* Brewfile.dev-containers â€” Docker CLI/Compose/Buildx + Colima + linters/scanners
* Brewfile.dev-iot â€” Arduino/ESP32 toolchain + serial tools
* Brewfile.cloudflare â€” cloudflared + wrangler + IaC helpers
* Brewfile.azure â€” Azure CLI + azcopy + kubernetes/AKS tooling + helpers

Adjust module membership by editing the corresponding Brewfile.